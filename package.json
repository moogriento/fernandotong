{
  "name": "fernandotong",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "predev": "rm -rf dist",
    "dev": "parcel index.html",
    "prebuild": "rm -rf dist",
    "build": "parcel build index.html --no-source-maps"
  },
  "devDependencies": {
    "parcel": "^2.12.0"
  }
}